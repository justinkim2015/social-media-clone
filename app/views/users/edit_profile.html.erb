<h2>Update Profile</h2>

<%= form_with model: @user do |f| %>
  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_area :bio, autocomplete: "bio" %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
