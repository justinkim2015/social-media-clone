<% if current_user == post.user %>
  <%= button_to "X", 
              post_path(post.id),
              method: :delete,
              class: "button is-small is-rounded is-danger mr-2" %>
<% end %>
